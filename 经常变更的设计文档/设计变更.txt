《设计文档_12-20》
1.更新了用例说明。
2.确定了数据库存储的实体集和属性集。

《客户端数据格式》：（见加粗部分）
二、任务调度
1.去掉了任务的“撤销理由”属性，增加了“获取单个任务详细信息+双方手机号”的指令接口。
2.任务相关的地址/时间属性有微调。
3.查看任务列表的具体指令（包括搜索/排序/查看跟自己相关的任务等）有大改。

《数据库数据格式》：（见加粗部分）
一、注册/登录/修改个人信息
1.给用户表加入“冻结的金币”属性。
2.删除了发送地址列表的功能。
3.新增发送“密码”信息。

二、任务调度
1.新增“获取单个任务详细信息+双方手机号”的指令接口。